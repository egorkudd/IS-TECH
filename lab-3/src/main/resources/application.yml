spring:
  datasource:
    url: jdbc:mysql://localhost:3306/itmo_lab2?useSSL=false
    username: root
    password: testtest
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
#  flyway:
#    baseline-on-migrate: true
#    check-location: true
#    locations: classpath:db/migration
#    schemas: public
#    enabled: true